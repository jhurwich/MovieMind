<h1>Listing genres</h1>

<table>
  <tr>
    <th>Name</th>
  </tr>

<% @genres.each do |genre| %>
  <tr>
    <td><%=h genre.name %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New genre', new_genre_path %>
